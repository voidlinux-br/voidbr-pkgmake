# Maintainer: Vilmar Catafesta <vcatafesta@gmail.com>

pkgname=xdg-desktop-portal-hyprland
pkgver=1.3.12
_protocolver=4d29e48433270a2af06b8bc711ca1fe5109746cd
pkgrel=2
pkgdesc="Backend of xdg-desktop-portal for Hyprland"
url="https://github.com/hyprwm/$pkgname"
arch=(x86_64)
license="BSD-3-Clause"
provides=()
depends=(
	libgcc
	libstdc++
	glibc
	hyprlang
	hyprutils
	hyprwayland-scanner
	inih
	pipewire
	qt6-base
	sdbus-c++
	util-linux-libs
	xdg-desktop-portal
)
makedepends=(
  cmake
	hyprland-protocols
	wayland
	wayland-protocols
  git
  hyprwayland-scanner
  pkgconf
  qt6-base-devel
  scdoc
  wayland-protocols
  elogind-devel
  hyprland-protocols
  hyprlang hyprutils
  hyprwayland-scanner
  libgbm-devel
  libdrm-devel
  pipewire-devel
  qt6-base-devel
  qt6-wayland-devel
  sdbus-c++
  wayland-devel
)
optdepends=('grim: required for the screenshot portal to function'
	'slurp: support for interactive mode for the screenshot portal')
_archive="$pkgname-$pkgver"
source=("$url/archive/v$pkgver/$_archive.tar.gz"
	"https://github.com/hyprwm/hyprland-protocols/archive/$_protocolver.tar.gz")
sha256sums=('c30ffe6ce9569a222438cb403d426a58c84e3653f19c8de641673097a9acc3b0'
	'5f6be4d870e94314f05ec7ff9c9c1f028748230ff80a7f89aeaf66c08e5c70e0')

prepare() {
	cd "$_archive/subprojects"
	rm -rf hyprland-protocols sdbus-cpp
	ln -sfT "$srcdir/hyprland-protocols-$_protocolver" hyprland-protocols
}

build() {
	cd "${srcdir}/$_archive"
	cmake -B build \
		-D CMAKE_INSTALL_PREFIX=/usr \
		-D CMAKE_INSTALL_LIBEXECDIR:PATH=/usr/libexec \
		-D CMAKE_BUILD_TYPE=Release
	cmake --build build
}

package() {
	cd "$_archive"
	DESTDIR="$pkgdir" cmake --install build
	install -Dm0644 -t "$pkgdir/usr/share/licenses/$pkgname/" LICENSE
}

pre_install() {
	log "Iniciando instalação"
}

post_install() {
	log "Finalizando instalação"
}

pre_upgrade() {
	log "Iniciando upgrade"
}

post_upgrade() {
	log "Finalizando upgrade"
}
