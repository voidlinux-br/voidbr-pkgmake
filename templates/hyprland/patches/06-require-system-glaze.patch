--- a/hyprpm/CMakeLists.txt
+++ b/hyprpm/CMakeLists.txt
@@ -11,19 +11,19 @@
 
 pkg_check_modules(hyprpm_deps REQUIRED IMPORTED_TARGET tomlplusplus hyprutils>=0.7.0)
 
-find_package(glaze 7...<8 QUIET)
+find_package(glaze 7...<8 QUIET CONFIG PATHS "/usr/share/glaze" NO_DEFAULT_PATH)
 if (NOT glaze_FOUND)
-    set(GLAZE_VERSION v7.2.0)
-    message(STATUS "hyprpm: glaze dependency not found, retrieving ${GLAZE_VERSION} with FetchContent")
-    include(FetchContent)
-    FetchContent_Declare(
-        glaze
-        GIT_REPOSITORY https://github.com/stephenberry/glaze.git
-        GIT_TAG ${GLAZE_VERSION}
-        GIT_SHALLOW TRUE
-        EXCLUDE_FROM_ALL
-    )
-    FetchContent_MakeAvailable(glaze)
+    find_package(glaze 7...<8 QUIET CONFIG)
+endif()
+
+if (NOT TARGET glaze::glaze)
+    find_path(GLAZE_INCLUDE_DIR NAMES glaze/glaze.hpp)
+    if (NOT GLAZE_INCLUDE_DIR)
+        message(FATAL_ERROR "glaze dependency not found; install system glaze package")
+    endif()
+
+    add_library(glaze::glaze INTERFACE IMPORTED)
+    target_include_directories(glaze::glaze INTERFACE "${GLAZE_INCLUDE_DIR}")
 endif()
 
 add_executable(hyprpm ${SRCFILES})
